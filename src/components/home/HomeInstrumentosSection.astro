---
import { pageGutter } from '../../data/layout';

/** Figma ICONES_FINALS — tira continua de 19 logos (4212×216). */
const logosStripSrc = '/images/instruments/icones-finals.png';
---

<section id="material" class="w-full bg-white py-8 md:py-14">
  <div class:list={[pageGutter, 'flex flex-col items-start md:items-end']}>
    <div class="w-full max-w-[84.375rem] text-left md:text-right">
      <h2 class="font-archivo text-[clamp(1.375rem,5vw,1.875rem)] leading-[1.15] font-light text-[#202d35]">
        Instrumentos y material disponible
      </h2>
    <a
        href="#contacto"
        class="font-archivo mt-2 inline-block text-base leading-[1.09] text-[#202d35] underline md:text-[18px]"
      >
        Saber más
      </a>
    </div>
  </div>

  <div class="logos-bar mt-[15px]" aria-label="Logos de marcas de instrumentos">
    <div class="logos-bar__track">
      <img
        src={logosStripSrc}
        alt="Neumann, Apogee, Gibson, Fender, Heritage Audio y más marcas disponibles"
        class="logos-bar__strip"
        width={4096}
        height={210}
        loading="eager"
        decoding="async"
      />
    </div>
  </div>
</section>

<style>
  .logos-bar {
    position: relative;
    overflow: hidden;
  }

  .logos-bar::after {
    content: '';
    position: absolute;
    inset-block: 0;
    inset-inline-end: 0;
    z-index: 1;
    width: clamp(4rem, 12vw, 8rem);
    background: linear-gradient(to right, transparent, #ffffff);
    pointer-events: none;
  }

  .logos-bar__track {
    overflow-x: auto;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    padding-inline: clamp(1rem, 4vw, 6rem) clamp(4rem, 12vw, 8rem);
  }

  .logos-bar__track::-webkit-scrollbar {
    display: none;
  }

  .logos-bar__strip {
    display: block;
    height: clamp(3.5rem, 12vw, 13.5rem);
    width: auto;
    max-width: none;
  }
</style>
